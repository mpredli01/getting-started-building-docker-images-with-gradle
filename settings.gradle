rootProject.name = 'getting-started-building-docker-images-with-gradle'

dependencyResolutionManagement {
    versionCatalogs {
        create("buildsrclibs") {
            from(files("./gradle/buildsrc.libs.versions.toml"))
            }
        }
    }
